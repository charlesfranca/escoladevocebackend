@model List<EscolaDeVoce.Services.ViewModel.ProjectViewModel>

@{
    ViewData["Title"] = "Projetos";
}

@section scripts {
    <script>
        $(document).ready(function() {
            $('#table').DataTable();
        });
    </script>
}

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Projetos</h3>
  </div>
  <div class="panel-body">
    <table id="table" class="table table-striped table-bordered" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Ações</th>
            </tr>
        </tfoot>
        <tbody>
        @foreach (var project in Model)
        {
            <tr>
                <td>@project.Id</td>
                <td>@project.name</td>
                <td>
                    <a asp-controller="Project" asp-action="Detail" asp-route-id="@project.Id"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span></a> | 
                    <a asp-controller="Project" asp-action="Delete" asp-route-id="@project.Id"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a>
                </td>
            </tr>
        }
        </tbody>
    </table>

  </div>
</div>